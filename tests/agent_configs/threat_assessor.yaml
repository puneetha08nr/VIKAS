agent_name: threat_assessor
tier: fast
uses_llm: false
output_table: competitor_content
has_rls: true
external_dependency: none
skip_concurrent: false

happy_path_params: {}  # reads competitor_content from DB, updates threat_score

row_count_sql: "SELECT COUNT(*) FROM competitor_content WHERE org_id = '{org_id}'"
expected_min_rows: 0
idempotent: true

api_read_endpoint: "GET /api/v1/competitor-content"
api_write_endpoint: ~
api_agent_trigger: "POST /api/v1/agents/threat_assessor/run"
api_invalid_body: ~
api_invalid_expected_status: ~

expected_list_fields: []
expected_nullable_fields: []

ui_page: /competitors
ui_component: "Threat score badges on competitor content"
ui_checklist:
  - "Competitor content rows show threat_score badge (low/medium/high)"
  - "Threat scores are between 0.0 and 1.0"
  - "High-threat content is visually highlighted"
