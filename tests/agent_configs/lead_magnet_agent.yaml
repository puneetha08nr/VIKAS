agent_name: lead_magnet_agent
tier: standard
uses_llm: true
output_table: lead_magnets
has_rls: true
external_dependency: none
skip_concurrent: true

happy_path_params:
  keyword: "ai marketing automation"
  format: "checklist"

row_count_sql: "SELECT COUNT(*) FROM lead_magnets WHERE org_id = '{org_id}'"
expected_min_rows: 0
idempotent: false

api_read_endpoint: ~
api_write_endpoint: ~
api_agent_trigger: "POST /api/v1/agents/lead_magnet_agent/run"
api_invalid_body: ~
api_invalid_expected_status: ~

expected_list_fields: []
expected_nullable_fields: []

ui_page: /content
ui_component: "Lead magnets section"
ui_checklist:
  - "Lead magnets appear with title and format badge (checklist/guide/etc.)"
  - "body content is readable text, not raw JSON"
  - "Format badge matches the requested format type"
