agent_name: newsletter_agent
tier: standard
uses_llm: true
output_table: newsletters
has_rls: true
external_dependency: none
skip_concurrent: true

happy_path_params:
  article_id: ~
  keyword: "ai marketing automation"

row_count_sql: "SELECT COUNT(*) FROM newsletters WHERE org_id = '{org_id}'"
expected_min_rows: 0
idempotent: false

api_read_endpoint: "GET /api/v1/newsletters"
api_write_endpoint: ~
api_agent_trigger: "POST /api/v1/agents/newsletter_agent/run"
api_invalid_body: ~
api_invalid_expected_status: ~

expected_list_fields: []
expected_nullable_fields: []

ui_page: /content
ui_component: "Newsletter tab"
ui_checklist:
  - "Newsletters appear with subject line and preview_text"
  - "body_html renders as formatted HTML preview (not raw HTML string)"
  - "Status badge visible (draft/approved)"
